TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
GOOGLE_OAUTH_TOKEN=          # OAuth user token JSON (run auth.py to generate); preferred for org-managed Google accounts
GOOGLE_SHEETS_CREDENTIALS=   # service-account JSON path or contents (fallback if not using OAuth)
ALCHEMY_API_KEY=             # optional, for Base decimals
HELIUS_API_KEY=              # optional, for Solana decimals
